<%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id),
              html: {method: :delete}) do |f| %>
  
  <%= f.submit "Unfollow", class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-16 rounded" %>
<% end %>